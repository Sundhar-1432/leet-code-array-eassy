class solution{
public:

    int findFloor(vector<int>& arr, int k) {
        int q=-1;
        for(int i=0;i<arr.size();i++){
            if(arr[i]<=k){
                q=i;
            }
        }
        return q;
    }
};
